<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.seven.seven">

    <permission
        android:name="com.seven.seven.permission.JPUSH_MESSAGE"
        android:protectionLevel="signature" />
    <!-- Required -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <application
        android:name=".ui.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/seven_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:name">

        <activity
            android:name=".ui.MainActivity"
            >

        </activity>
        <activity
            android:name=".ui.SplashActivity"
            android:theme="@style/FlashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.TestActivity" />

        <activity android:name=".ui.base.activity.ConstantsLayout" />
        <activity android:name=".ui.base.activity.ViewTestActivity" />
        <activity android:name=".home.HomeNewsDetailActivity" />
        <activity android:name=".login.LoginActivity" />
        <activity android:name=".user.CollectListActivity" />
        <activity android:name=".search.SearchDetailActivity" />
        <activity android:name=".login.RejisterActivity" />
        <activity android:name=".search.LookImageActivity"/>
    </application>

</manifest>